{
    "mcpServers": {
        "notes-app": {
            "type": "http",
            "url": "http://localhost:3000/mcp",
            "headers": {
                "Authorization": "Bearer ${MEMORY_AGENT_TOKEN}"
            }
        }
    }
}